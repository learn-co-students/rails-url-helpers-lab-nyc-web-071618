<h1><%= @student.to_s %></h1>
<% if @student.active %>
  This student is currently active.
<% else %>
  This student is currently inactive.
<% end %>

<a href="/students/<%= @student.id %>/activate">ACTIVATE!!!!</a>
<%= link_to "Activate awesomeness", activate_student_path(@student.id) %>
